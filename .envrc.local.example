# Backend debugging with delve
# Set to "true" to enable delve debugger on port 2345
# Also enables mage-watcher for auto-rebuild on Go file changes
export DEVELOPMENT=true

export LOCAL_REGISTRY=localhost:5000
export PLUGIN_ID=ajwelch-x-app
